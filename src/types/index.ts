export interface UserInfo {
    id: string;
    name: string;
    nickname: string;
    type: string;
    password: string;
    class: string;
}

export interface LoginInfo {
    id: string;
    type: string;
    password: string
}